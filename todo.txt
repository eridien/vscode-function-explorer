bugs ...
  marked-only on at boot
  sel subfunc selects parents
  funcs not loaded at boot

todo ...
  reveal mark when using item icon click to turn on mark
  handle file filtered with no marks?
    don't close?
    show message?
    item indicator?
  turn off filter
    when file opens (collapsed state is in item)
    when no marks
  don't show files with no funcs
    use stored funcs
  many settings don't immediately change sidebar
  focus editor with a mark on prev/next
  add classes to func list
  show parent chain in func label
    including methods b / a @ c
  handle fs changes
  flash line (entire function?) when revealed

unlikely ...
  custom scroll
  keep mark during cut/paste
  has mark indicator on file
    
  
options ...
  reveal func item when text selected  (sidebar already visible)
  reveal func item when toggled on     (sidebar already visible)

testing ...
  settings changes
  error test files

release checks ...
  version
  log('
  todo
  console.log
  debug
  dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
