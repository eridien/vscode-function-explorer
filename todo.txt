bugs ...
  one line file doesn't toggle: const x = () => {};
  func parentid missing
  no pointer on boot
  pointer isn't removed from old file
  can't toggle: function x(){}
  toggle on with selection
  minimal scrolling broken

todo ...
  focus editor on any keyboard cmd
  add classes to func list
  click on folder to reveal all funcs below
  dbl-click on fn name in text toggles
  show parent chain in func label
    including methods b / a @ c
  toggle mark on func item dbl-click
  clean up updates to tree
  handle fs changes
  set pointer on click in body of func
  alpha sort setting doesn't immediately change sidebar
  flash line (entire function?) when revealed

unlikely ...
  make prev/next when not in valid file go to valid mark
  custom scroll
  keep mark during cut/paste
  
options ...
  reveal func item when text selected  (sidebar already visible)
  reveal func item when toggled on     (sidebar already visible)

testing ...
  settings changes
  error test files

release checks ...
  version
  log('
  todo
  console.log
  debug
  dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
