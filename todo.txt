notes...
  moving to different context kills mark

release changes ...

bugs ...
  adding foo(){} to clear() -> no foo and hilite pos doesn't change
    can't repeat
  
low priority ...
  toggling open filter with no marks 
    -> always turn off filter and show all
  adding test.ts with file explorer doesn't show
  itemsById in description in itemsById 
        b-dbs.ts: private static itemsById
  chg alpha sort -> no refresh
  too many matches per mark
    bookmarks - extension.ts
    only set one mark per toggle
      option: use line number with marks

todo ...
  toggle all in selection
  rgtest in rust needs context
  add typedef struct to C lang
  filter addDir and add by include()
    .unwatch(path / paths)
  languages

unlikely ...
  moving to different context kills mark
  bounce on some func clicks
  incremental parsing
  chg funcnodes to use indexes not lines
    solves multiple items in one line

testing ...
  settings changes 
  syntax errors
  languages
  toggling around neighbor
  toggling two funcs in one line
  adding, deleting, moving, and copying funcs
  changing files in other editor
  marks/gutter across boots
  dittos
  multiple ws roots
  check all menus
  check item icon buttons
  multiple selections
  dark theme
  gesture
  dbl-click name in editor

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg debugger
  check problems
  run and check console
  CLEAR_MARKS_ON_STARTUP
  PARSE_DUMP_TYPE
  PARSE_DUMP_NAME
  PARSE_DEBUG_STATS


npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
