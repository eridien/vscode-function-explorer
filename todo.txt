bugs ...
  can't repeat ...
    show nodes wrong places late in file
    ...functionTopMargin
    = err
    next broken in demo folder
      one mark in _init_.py
      collapse all
      next broken
    cut/paste leaves mark behind

  ts: export interface NodeData {
        funcId: string;
      }
  c: struct RenameToken {
       RenameToken *pNext; 
     };
    #ifdef SQLITE_DEBUG



todo ...
  clnup languages
  find all syntax names and space them
  select dbl-click name
  next/prev leave selection at start of name
  handle beforeAfter at beg/end
  funcitem getDescription

low priority ...
  delete file leaves folder

unlikely ...
  multiple items in one line
  bounce on some func clicks
  incremental parsing
  chg funcnodes to use indexes not lines
    solves multiple items in one line
  gutter highlight skips blank lines

testing ...
  settings changes
  error test files
  adding, deleting, moving, and copying funcs
  changing files in other editor
  multiple ws roots
  multiple selections
  mark persistence
  dark theme

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
