bugs ...
  class repeated many times with missing letters (start/end wrong)
  left name in class exp assign not clickable
  overlap broken in nesting, clicks and toggle
  deleting marked function leaves mark on next func
  class item mark removed with item click
  remove marks in folder (X) broken

todo ...
  when toggle in text and no func, find one
    use to find more syntax funcs
  handle fs changes
  support multiple funcs with same id
  add "Fit Function (with margin)" as scroll option
  focus editor with a mark on prev/next
  collapse empty folders into one
  many settings don't immediately change sidebar

options ...
  reveal func item when toggled on     (sidebar already visible)

testing ...
  settings changes
  error test files
  adding, deleting, moving, and copying funcs
  changing files in other editor
  multiple selections
  mark persistence
  dark theme

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
