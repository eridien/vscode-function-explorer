bugs ...
  many settings don't immediately change sidebar
  file changes ignored
  bounce on some func clicks

todo ...
  default to alpha
  when no marks always show all funcs under file item
  chg funcnodes to use indexes not lines
    solves multiple items in one line
    fixes priority behavior
  toggling if(distFromSelToAround <= distFromSelToNext) makes many marks
    add single add-mark filter for items and gutter
  don't refresh item from single click selection pointer changes?
  add call to capture
  handle fs changes
  handle multiple marks in line
  sel name when side open -> reveal item & set stayalive
  incremental parsing
  darken hilite
  chg name click toggle to gesture
  finish sidebar refresh command/menu

options ...
  reveal item when marking
  open in preview mode
  default file wrap off
  when adding mark then always scroll

testing ...
  settings changes
  error test files
  adding, deleting, moving, and copying funcs
  changing files in other editor
  multiple ws roots
  multiple selections
  mark persistence
  dark theme

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
