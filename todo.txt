bugs ...
  bounce on some func clicks
  getlabel not in alpha sort   disp.ts
  multiple ws roots
  center-if-needed on big funcs over top
  next on toggle, line 45 in misc/webview-msg-ext.js
  pointer not updated on func click
  const x = function () not captured
  no mark at getStartLine in disp.ts (FileItem getChildren works)

todo ...
  handle multiple marks in line
  sel name when side open -> reveal item & set stayalive
  incremental parsing
  darken hilite
  chg name click to gesture
  if name not visible when adding mark then scroll
  toggle closer
  handle sel overlapping name line only
  sidebar refresh command/menu
  handle fs changes
  many settings don't immediately change sidebar

options ...
  open in preview mode
  default file wrap off
  when adding mark then always scroll

testing ...
  settings changes
  error test files
  adding, deleting, moving, and copying funcs
  changing files in other editor
  multiple ws roots
  multiple selections
  mark persistence
  dark theme

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
