bugs ...
  many settings don't immediately change sidebar
  bounce on some func clicks
  delete file leaves folder
  file with no marks shows empty in filtered mode and stuck, box.ts 
  
todo ...
  is loadPaths cleared?
  deleteFuncById should also remove func from set
  don't show syntax errors in info
  finish sidebar refresh command/menu
  more languages

unlikely ...
  incremental parsing
  chg funcnodes to use indexes not lines
    solves multiple items in one line
    fixes priority behavior

options ... 
  explorer compression
    nested folders and files
    no root folders
    flattened folders
    file list, no folders
    function list, no files
  reveal item when marking
  open in preview mode
  default file wrap off
  when adding mark then always scroll

testing ...
  settings changes
  error test files
  adding, deleting, moving, and copying funcs
  changing files in other editor
  multiple ws roots
  multiple selections
  mark persistence
  dark theme

release checks ...
  package.json version
  log
  console.log
  todo to-do
  debug dbg
  check problems
  run and check console

npm install -g @vscode/vsce
vsce package
https://marketplace.visualstudio.com/manage/publishers/
